import styles from './404.module.css'
import Head from 'next/head'

import BoxOpenIcon from '../components/Icons/BoxOpenIcon'

import Container from '../components/Container'
import Button from '../components/Button'

export default function Error404() {
  return (
    <>
      <Head>
        <title>SM Logística</title>
        <meta name='description' content='Generated by create next app' />
        <link rel='icon' href='/favicon.ico' />
      </Head>
      <Container>
        <div className={styles.content}>
          <h1 className={styles.code}>
            4<BoxOpenIcon width={180} />4
          </h1>
          <h2 className={styles.title}>¡Ups! No podemos encontrar la página que estabas buscando</h2>
          <p className={styles.info}>El enlace que seguiste no existe o esta roto</p>
          <div className={styles.button}>
            <Button href={'/'} className={'buttonPrimary'}>
              Volver al inicio
            </Button>
          </div>
        </div>
      </Container>
    </>
  )
}
